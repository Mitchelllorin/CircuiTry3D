# Production environment — GitHub Pages web deployment is the demo version.
# The full component library ships with the Play Store / Android release.
VITE_DEMO_MODE=true
# VITE_OWNER_KEY_HASH is injected by the deploy workflow from the
# OWNER_KEY_HASH GitHub Actions secret (SHA-256 hex of the owner password).
