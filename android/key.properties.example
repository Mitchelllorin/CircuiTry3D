## Example signing config (DO NOT COMMIT real secrets)
##
## Copy to `android/key.properties` and fill in your values.
## Keep the keystore file local and backed up securely.
##
## storeFile is relative to android/app (because app/build.gradle uses `file(...)`).
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=YOUR_KEY_ALIAS
storeFile=keystore/your-upload-keystore.jks

