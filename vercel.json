{
  "projectSettings": {
    "framework": "vite",
    "installCommand": "npm ci",
    "buildCommand": "npm run build",
    "outputDirectory": "dist"
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "git": { "deploymentEnabled": { "production": true, "preview": true } },
  "rootDirectory": "frontend"
}
